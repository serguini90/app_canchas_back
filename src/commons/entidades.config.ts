import { Usuario } from "src/entities/usuario.entity";

export function obtenerEntidades(){
    return [Usuario];
}

/* export function obtenerLista(lista: string){
    switch(lista.toLowerCase()){
      case Listas.LISTAROL:
        return ListaRol;
    }
} */